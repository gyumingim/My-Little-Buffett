# My Little Buffett - 버핏형 가치투자 스크리너

워런 버핏의 가치투자 원칙 기반 한국 주식 스크리닝 시스템

## 📦 프로젝트 구조

이 프로젝트는 **데이터 수집**과 **웹사이트**가 완전히 분리되어 있습니다:

1. **데이터 수집 스크립트** (`fetch_dart_data.py`) - DART API 호출 → CSV 저장
2. **웹사이트** (`backend/` + `frontend/`) - CSV만 읽어서 분석/표시 (API 호출 없음)

## 🚀 사용 방법

### 1단계: 데이터 수집 (별도 실행)

```bash
# 2024년 데이터 100개 기업 수집 (배치 크기 10, 대기 1초)
python fetch_dart_data.py --year 2024 --limit 100 --batch-size 10 --delay 1.0
```

**옵션:**
- `--year`: 사업연도 (필수)
- `--limit`: 수집할 기업 수 (기본: 100)
- `--batch-size`: 동시 요청 수 (기본: 10)
- `--delay`: 배치 간 대기 시간 초 (기본: 1.0)

### 2단계: 웹사이트 실행

```bash
docker-compose up -d
```

브라우저에서 http://localhost 접속

## 📊 현재 데이터

- CSV 파일: 8,912개 보존됨
- 2024년: 7,881개
- 2023년: 1,031개
